from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This repo it's destinate for players of Age of Empires II that wants to see the probability of a civilization wins then other civilization, based a dataset ranked matches.',
    author='nicolas-thomazini',
    license='MIT',
)
